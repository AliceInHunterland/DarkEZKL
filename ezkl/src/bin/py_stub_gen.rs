//! Minimal `py_stub_gen` binary target.
//!
//! `Cargo.toml` declares a `[[bin]]` named `py_stub_gen` without an explicit
//! `path`, so Cargo expects `src/bin/py_stub_gen.rs` to exist.

fn main() {
    // Intentionally minimal for this kata snapshot.
}
